<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bsoft.checkappointment">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">
        <activity
            android:name=".common.ChooseFamliyActivity"
            android:theme="@style/MyAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".outpatients.activity.OutPatientsAppointActivity"
            android:theme="@style/MyAppTheme" />
        <activity
            android:name=".common.ConfirmAppointActivity"
            android:theme="@style/MyAppTheme" />
        <activity
            android:name=".common.CancelAppointActivity"
            android:theme="@style/MyAppTheme" />
        <activity
            android:name=".common.ChooseAppointTimeActivity"
            android:theme="@style/MyAppTheme" />
    </application>

</manifest>